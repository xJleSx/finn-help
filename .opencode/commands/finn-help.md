---
description: Load the finn-help mega-skill with all project conventions (Python, TypeScript, SQL, testing, trading, MOEX)
---

First, load the project skill by running the `skill` tool:

```
skill finn-help
```

Then provide a structured overview of the project based on the loaded skill: architecture, stack, conventions, available commands (`/finn-review`, `/finn-market`, `/finn-trade`, `/finn-ml`), and the 4 subagents (finn-reviewer, finn-architect, finn-db, finn-debug). Ask which area they want to work on.
